# MySQL container
MYSQL_ROOT_PASSWORD=pokemon123
MYSQL_DATABASE=pokemon_db

# Web app database connection (use same as root for local dev, or create a dedicated user)
DB_USER=root
DB_PASSWORD=pokemon123
DB_NAME=pokemon_db

# Session secret for auth (change in production)
SESSION_SECRET=your-secret-key-change-in-production
