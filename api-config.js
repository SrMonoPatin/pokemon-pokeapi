/**
 * API base URL for auth and custom Pokemon.
 * - Empty string: same origin (Docker local, or when frontend is served from backend)
 * - Set to your Railway URL when using GitHub Pages (see DEPLOY.md)
 */
var API_BASE = 'https://pokemon-pokeapi-production.up.railway.app';
